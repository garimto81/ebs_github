name: ebs_game_engine
description: EBS Poker Game Engine — Flop family 7 variants + Interactive Simulator
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  yaml: ^3.1.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
