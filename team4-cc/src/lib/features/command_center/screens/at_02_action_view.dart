// AT-02 Action View — action buttons (BS-05-02, CCR-031).
// Dynamic label switching (CHECK↔CALL, BET↔RAISE-TO) based on BiggestBet.
// Buttons are disabled until ActionOnResponse received from server.

import 'package:flutter/material.dart';

class At02ActionView extends StatelessWidget {
  const At02ActionView({super.key});

  @override
  Widget build(BuildContext context) => const Placeholder();
}
